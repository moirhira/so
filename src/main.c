#include "../libraries/minilibx-linux/mlx.h"
#include "../includes/so_long.h"
#include <stdlib.h>

#define WIDTH   600
#define HIGHT   400


int main() {
    read_map();
    // void *mlx_ptr;
    // void *win_ptr;


    // mlx_ptr = mlx_init();
    // if (!mlx_ptr)
    //     return (1);

    // win_ptr = mlx_new_window(mlx_ptr, WIDTH, HIGHT, "Game : moirhira");
    // if (!win_ptr)
    //     return (free(mlx_ptr),1);
    // mlx_loop(mlx_ptr);
    // mlx_destroy_window(mlx_ptr,win_ptr);
    // mlx_destroy_display(mlx_ptr);
    // free(mlx_ptr);
    return 0;
}